<ul>
  <% @articles.each do |article|%>
    <li>
      <%= link_to article.title, article_path(article) %>
    </li>
  <% end %>
</ul>

<%= link_to new_article_path, class: 'btn btn-secondary' do %>
  Add a new article
<% end %>
